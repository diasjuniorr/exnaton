import { Outlet } from "react-router-dom"

const Navigation = () => {
    return (
        <>
        <div>
            nav
        </div>
        <Outlet/>
        </>
        
    )
}

export default Navigation